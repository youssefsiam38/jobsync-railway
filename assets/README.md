# Assets

- `icon.png` — the template's own generic icon (a simple tracked-checklist). It does **not** reproduce the JobSync
  logo or any JobSync brand identifier. Referenced by the published template via a raw GitHub URL to a tagged commit
  (see `RAILWAY_TEMPLATE.md`).
